files:
<% node['remote_syslog2']['config']['files'].each do |file| %>
  - <%= file %>
<% end %>
destination:
  host: <%= node['remote_syslog2']['config']['destination']['host'] %>
  port: <%= node['remote_syslog2']['config']['destination']['port'] %>
  protocol: tls
exclude_patterns:
<% node['remote_syslog2']['config']['exclude_patterns'].each do |pattern| %>
  - <%= pattern %>
<% end %>
